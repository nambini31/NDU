using System;
using System.Collections.Generic;
using System.Text;

namespace SAIM.Core.Utilities
{
    //public static (string Controller, string Action) ParseUrl(string url)
    //{
    //    if (string.IsNullOrEmpty(url))
    //        return (null, null);

    //    // Supprime le premier "/" et divise l'URL en parties
    //    var segments = url.TrimStart('/').Split('/');

    //    if (segments.Length >= 2)
    //    {
    //        return (segments[0], segments[1]); // [Controller, Action]
    //    }

    //    return (null, null);
    //}
}
